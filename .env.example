# Public env vars (exposed to the browser, prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Vercel Analytics (auto-detected when deployed to Vercel, leave empty locally)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=
